# HealthBridge Development Guidelines

## 適用範囲

このファイルは、リポジトリ全体に適用する開発ガイドラインです。

## 基本方針

- このアプリは、応募企業へ提出するポートフォリオである。
- 可読性、説明可能性、安全性を優先する。
- 指定された範囲を超えて変更を広げない。
- ユーザーが作成した変更や既存の未コミット変更を、勝手に破棄・上書きしない。
- 変更前に、関連する実装コードと文書を確認する。
- 実装と文書の不一致を放置しない。
- 未確定の仕様を確定事項として実装しない。
- 不明点によって実装結果が大きく変わる場合は、作業前にユーザーへ確認する。
- 現在のMVPから除外された機能を、過去資料だけを根拠として復活させない。

## 判断時に優先して確認する情報

仕様や実装方針を判断するときは、原則として次の順で確認する。

1. 現在の実装コード
2. `docs/requirements.md`
3. `docs/architecture.md`
4. `docs/review-history.md`
5. `README.md`
6. Googleドキュメントなどの過去資料

- 文書間に不一致がある場合は、独自判断で一方を採用せず、差分を報告する。
- GitHub Issueの内容が現在の実装や上記文書と異なる場合は、Issueをそのまま最新仕様として扱わない。
- 過去資料にある機能を、現在のMVPへ自動的に復活させない。
- 「実装済み」「設計済み」「予定」「未実装」を明確に区別する。

## 作業開始前の確認

- 現在のブランチ、追跡先、`git status --short`を確認する。
- `AGENTS.md`と、作業対象のディレクトリ内に追加の指示ファイルがないか確認する。
- 対応するIssue、関連コード、テスト、文書を確認する。
- 未コミット変更がある場合は内容を確認し、今回の作業と重なるときは作業前に報告する。
- Formatter導入前または設定未確認の状態で、フロントエンド全体を一括整形しない。

## Git・Issue・PR

- 作業は原則としてIssue単位で行う。
- Issueに対応した専用ブランチを使用する。
- 機能変更と機械的な整形を同じPRへ混在させない。
- Formatterによる変更と、リファクタリングや挙動変更を同じコミットへ含めない。
- commit、push、Issue・PRに対する操作は、ユーザーから明示的な指示がある場合のみ行う。
- ユーザーが作成した変更や既存差分を勝手に戻さない。
- `git reset --hard`、強制pushなどのdestructiveなGit操作を行わない。
- PRには、変更理由、変更内容、検証結果、未確認事項を記載する。
- 画面変更を含む場合は、可能な範囲でスクリーンショットを添付する。
- 作業終了時に、変更ファイル、検証結果、未確認事項、`git status --short`を報告する。

## コードの可読性

- 適切なインデントと一貫したフォーマットを維持する。
- 変数名・関数名から役割が分かるようにする。
- 関数を必要以上に長くしない。
- 重複した処理を放置しない。
- コメントには、コードだけでは分かりにくい実装意図や制約を書く。
- コードを読めば分かる自明な処理へ、過剰なコメントを追加しない。
- エラーを握りつぶさない。
- 一時的な回避策を恒久対応のように扱わない。
- 型エラーを安易に`any`で回避しない。

## フロントエンド

- フロントエンドの整形には、リポジトリで定めたFormatterを使用する。
- インデント、改行、引用符などを手作業だけで統一しない。
- 新規作成・変更したファイルは、FormatterとESLintの両方を通す。
- 機能実装のPRで、変更対象外のファイルを一括整形しない。
- 全体整形は、専用Issue・ブランチ・PRで実施する。
- import順やTailwind CSSのクラス順は、設定済みツールの規則に従う。
- ESLint無効化コメントは、必要性を説明できる場合に限って使用する。
- 依存関係を変更する場合は目的を明記し、`package.json`とlockfileを同時に更新する。
- Next.js、React、TypeScriptの既存構成を維持する。構成変更が必要な場合は、実施前に理由と影響範囲を確認する。
- UI変更時は、PCとスマートフォンの両方を確認する。
- hydration error、console error、page errorが発生していないことを確認する。
- 初回の全体整形で見つかった設計・ロジック上の問題は同時に修正せず、別Issue候補として報告する。

## バックエンド

- Go標準の`gofmt`に従う。
- 業務APIはGin、DBアクセスはGORM、DBはPostgreSQLを前提とする。
- handler、service、repositoryなどの責務を明確にする。
- HTTP固有の処理をserviceやrepositoryへ持ち込まない。
- DB操作をhandlerへ直接記述しない。
- ドメインルールと認可条件を、service層などの適切な場所で表現する。
- リクエストDTO、レスポンスDTO、DBモデルは必要に応じて分離する。
- パスワードを平文で保存しない。
- クライアントから送信されたrole、user ID、manager IDを無条件に信用しない。
- エラーレスポンス形式を統一する。
- ログへパスワード、セッションID、トークンなどの認証情報を出力しない。
- マイグレーションとモデル定義を同期させる。
- 認証・認可を画面側だけで完結させない。
- managerが参照・更新できるemployeeを、現在の担当関係によって制限する。
- employeeは本人の記録だけを参照・登録できるようにする。
- 無効化済みemployeeの過去記録は保持する。
- 通常の一覧表示では、無効化済みemployeeを除外する。
- 未入力は体調値として保存せず、対象記録が存在しない状態として扱う。
- 認証方式、manager登録資格、日付・タイムゾーンなどの未確定事項を、独自判断で実装しない。

## テスト・検証

フロントエンド変更後は、原則として次を実行する。

```bash
cd frontend
npm run format:check
npm run lint
npm run build
```

バックエンド変更後は、原則として次を実行する。

```bash
cd backend
gofmt -w path/to/changed.go
go test ./...
go vet ./...
```

共通の差分確認として、リポジトリルートで次を実行する。

```bash
git diff --check
git diff --stat
git status --short
```

- `gofmt`の対象は、今回変更したGoファイルに限定する。
- 変更内容に応じたテストを追加・更新する。
- 認証・認可では、未認証時の401、権限不足時の403、担当外employeeへのアクセス拒否を確認する。
- 実行できなかった検証は成功したように扱わず、理由を報告する。
- テスト失敗やlint・buildエラーを、抑制だけを目的とした設定変更で回避しない。
- テストのためだけに、本番コードへ不自然な分岐を追加しない。

## 文書更新

- 機能、API、DB、認証、認可を変更した場合は、必要に応じて`README.md`、`docs/requirements.md`、`docs/architecture.md`、`docs/review-history.md`、ER図、画面画像を更新する。
- 文書を実装より先に完成扱いにしない。
- 文書では「実装済み」「設計済み」「予定」「未実装」を明確に区別する。
- 文章は過剰に長くせず、本人が面接で説明できる自然な表現にする。

## 作業終了時の報告

次の形式で報告する。

### 変更内容

- 変更したファイル
- 各ファイルで行ったこと
- 実装上の判断理由

### 維持した仕様

- 変更していない挙動
- 既存要件との整合性

### 検証結果

- 実行したコマンド
- 成功・失敗
- ブラウザー確認内容
- テスト結果

### 未確認・判断が必要な事項

- 実行環境上確認できなかったこと
- 仕様判断が必要なこと
- 別Issue候補

### Git状態

```bash
git diff --check
git diff --stat
git status --short
```

必要に応じて、コミットメッセージ候補も提示する。

## 禁止事項

- 既存変更の無断破棄
- 指定外ファイルの不要な変更
- 未確定仕様の独断実装
- テスト失敗の無視
- lintやbuildエラーの抑制だけを目的とした設定変更
- 型エラーを`any`で回避すること
- 認可をフロントエンドだけで済ませること
- パスワードの平文保存
- 秘密情報のコミット
- 実データや個人情報をスクリーンショットへ含めること
- sudoやOS設定変更の無断実行
- 新しい依存関係の目的なき追加
- 大量の機械的変更と機能変更の混在
- commit、push、Issue・PR操作の無断実行
